// ============================================================
// Paste your AdSense ad-unit slot IDs here once you create them
// (AdSense -> Ads -> By ad unit -> Display ad -> responsive).
// Each one is a plain numeric string, e.g. "1234567890".
//
// Leave any of these as "" (empty string) and that placement
// simply won't render an ad yet — nothing breaks.
// ============================================================

export const AD_SLOTS = {
  /** Shows every few cards inside the signals feed (Home page) */
  signalsFeed: "",

  /** Shows on the Results / Signal Accuracy page */
  resultsPage: "",
};
